{% extends 'base.html.twig' %}

{% block title %}Utilisation{% endblock %}

{% block body %}
    <h1>Utilisation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ utilisation.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ utilisation.libelle }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ utilisation.image }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ utilisation.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_crud_utilisation_index') }}">back to list</a>

    <a href="{{ path('app_admin_crud_utilisation_edit', {'id': utilisation.id}) }}">edit</a>

    {{ include('admin_crud_utilisation/_delete_form.html.twig') }}
{% endblock %}
