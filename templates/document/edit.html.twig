{% extends 'admin.html.twig' %}

{% block title %}Mettre à jour un document{% endblock %}

{% block body %}
    <h1>Mettre à jour un document</h1>

    {{ include('document/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_document_index') }}">Retour à la liste des documents</a>

    {{ include('document/_delete_form.html.twig') }}
{% endblock %}
