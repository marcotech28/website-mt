{% extends 'base.html.twig' %}

{% block title %}Avantage
{% endblock %}

{% block body %}
	<h1>Avantage</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ avantage.id }}</td>
			</tr>
			<tr>
				<th>Libelle</th>
				<td>{{ avantage.libelle }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_admin_crud_avantage_index') }}">Retour à la liste des avantages</a>

	<a href="{{ path('app_admin_crud_avantage_edit', {'id': avantage.id}) }}">Modifier</a>

	{{ include('admin_crud_avantage/_delete_form.html.twig') }}
{% endblock %}
