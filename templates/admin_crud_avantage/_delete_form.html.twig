<form method="post" action="{{ path('app_admin_crud_avantage_delete', {'id': avantage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avantage.id) }}">
    <button class="btn">Delete</button>
</form>
