{% extends 'base.html.twig' %}

{% block title %}Mettre à jour mon mot de passe
{% endblock %}

{% block body %}
	<div class="container-xl">
		<h1>Mettre à jour mon mot de passe</h1>

		<div class="row">
			<div class="col-8">
				{{ form_start(resetForm) }}
				{{ form_row(resetForm.password) }}
				<button class="btn btn-primary" style="margin: 10px 0 50px 0">Je mets à jour mon mot de passe</button>
				{{ form_end(resetForm) }}
			</div>
		</div>
	</div>
{% endblock %}
